# 后端实施规划

## 目标

提供一个单机可部署的 Go 服务，允许注册用户在 AutoBuff 监控模式中登录，创建自己的监控会话和不可猜测的预览链接，并将纯标注数据实时转发给网页。本地软件内监控不依赖远程账号。

## 技术栈

- Go 1.22+
- `net/http`
- `github.com/coder/websocket`
- MySQL 8
- JWT 登录令牌
- bcrypt 密码哈希
- Nginx 反向代理

## 阶段

1. 数据库与配置：用户表、监控会话表、环境变量。
2. 账号体系：开放注册、登录、当前用户查询、注册开关。
3. 会话体系：创建/读取/撤销预览链接。
4. 实时通道：设备发布端、网页订阅端、最新状态快照。
5. AutoBuff 接入：Swift 登录、会话创建和监控帧发布。
6. 生产加固：HTTPS、限流、审计、密钥轮换和自动部署。
7. 通知体系：登录用户绑定加密渠道凭证，预览 Token 控制独立规则，后台异步发送并记录结果。
8. 规则状态：高频告警的计时和连续次数写入 MySQL，服务重启后继续累计。

## 权限边界

- 只有已登录用户可以创建监控会话。
- 发布端必须携带用户 JWT，且只能发布自己名下的会话。
- 查看端必须持有预览链接中的随机 Token。
- 服务端不接收任何游戏截图。
- 关闭注册不影响已有账号登录。
- 管理员在数据库中禁用账号后，新的 API 请求和 WebSocket 握手会立即被拒绝。
- Bark DeviceKey 只允许登录后写入并加密保存，预览接口不会返回明文凭证。
- 预览 Token 可以读取和切换通知规则，但测试通知有独立频率限制。
